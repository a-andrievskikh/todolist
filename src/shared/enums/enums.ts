export enum TaskStatuses {
  New = 0,
  InProgress = 1,
  Completed = 2,
  Dragt = 3,
}

export enum TaskPriorities {
  Low = 0,
  Middle = 1,
  High = 2,
  Urgently = 3,
  Later = 4,
}

export enum ResultCodes {
  Success = 0,
  Failed = 1,
  Captcha = 10,
}